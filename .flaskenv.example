# Flask application entry point (your run file)
FLASK_APP=run.py

# Flask environment: development | production | testing
FLASK_ENV=development

# Enable Flask debugger (1 = enabled, 0 = disabled)
FLASK_DEBUG=1

# Port number Flask will run on
FLASK_RUN_PORT=8080
